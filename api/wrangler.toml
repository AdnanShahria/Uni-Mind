name = "unimind-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
# Non-sensitive configuration variables.
# Wrangler automatically loads database and secret credentials from .dev.vars in local development.
